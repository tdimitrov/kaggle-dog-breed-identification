Dependencies:
pip install h5py

Intended usage:
0. params.py - Constants used in the whole project. Edit this file to set preferred filenames, locations, etc.
1. get_data.sh - Script which downloads training data and extracts it.
    1.1 convert.py - Convert input images to numpy arrays. (called by get_data.sh)
2. train.py - Trains a NN which uses ResNet50's features as input and produces a prediction.
3. predict.py - Generates submission file
